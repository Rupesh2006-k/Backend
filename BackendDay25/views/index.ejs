<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/styleSheets/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body class="bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen p-6">
    <div class="max-w-5xl mx-auto space-y-10">
      <div
        class="backdrop-blur-xl bg-white/60 shadow-xl border border-white/40 rounded-3xl p-8"
      >
        <h2 class="text-2xl font-bold mb-4 text-gray-800">Create a New Task</h2>

        <form class="space-y-5" action="/create" method="post">
          <input
            name="title"
            type="text"
            placeholder="Title your task"
            class="w-full px-4 py-3 rounded-2xl bg-white/70 border border-gray-300 shadow-sm focus:ring-2 focus:ring-indigo-400 focus:bg-white transition"
          />

          <textarea
            name="details"
            placeholder="Enter your task description"
            class="w-full px-4 py-3 h-36 rounded-2xl bg-white/70 border border-gray-300 shadow-sm resize-none focus:ring-2 focus:ring-indigo-400 focus:bg-white transition"
          ></textarea>

          <button
            type="submit"
            class="w-full py-3 bg-indigo-600 hover:bg-indigo-700 text-white font-semibold rounded-2xl shadow-lg active:scale-[.98] transition"
          >
            Create Task
          </button>
        </form>
      </div>

      <div class="flex flex-wrap gap-8 justify-start">
        <% if (files.length > 0) { %> <% files.forEach((elem) => { %>

        <div
          class="w-72 bg-white rounded-3xl p-6 border border-gray-200 hover:shadow-2xl hover:-translate-y-1 transition-all duration-300 shadow-md"
        >
          <h1 class="text-xl font-semibold text-gray-800 mb-4 break-all">
            <span class="text-gray-500 font-medium">File:</span>
            <%= elem %>
          </h1>
          <div
            class="flex justify-between items-center mt-3 text-sm font-medium"
          >
            <a
              href="/file/<%= elem %>"
              class="text-indigo-600 hover:text-indigo-700 hover:underline transition"
            >
              Read →
            </a>
            <a
              href="/edit/<%= elem %>"
              class="text-yellow-600 hover:text-yellow-700 hover:underline transition"
            >
              Edit ✎
            </a>
            <a
              href="/delete/<%= elem %>"
              class="text-red-600 hover:text-red-700 hover:underline transition"
            >
              Delete ✕
            </a>
          </div>
        </div>
        <% }); %> <% } else { %>
        <div class="w-full text-center py-10">
          <h1 class="text-2xl font-semibold text-gray-600">No Tasks Yet 😴</h1>
          <p class="text-gray-500 mt-2">Start by creating your first task!</p>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
