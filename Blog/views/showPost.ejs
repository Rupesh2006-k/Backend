<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= blog.title %> | Show Post</title>
  <!-- ✅ Correct Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-900 flex items-center justify-center min-h-screen p-6">

  <main class="max-w-3xl w-full bg-white shadow-lg rounded-2xl p-8">
    <h1 class="text-3xl font-bold mb-3 text-indigo-700">
      <%= blog.title %>
    </h1>

    <p class="text-sm text-gray-500 mb-6">
      ✍️ <%= blog.author %> • <%= blog.date %>
    </p>

    <p class="text-gray-700 leading-relaxed mb-6">
      <%= blog.content %>
    </p>

    <!-- Tags -->
    <div class="flex flex-wrap gap-2 mb-6">
      <% blog.tags.forEach(tag => { %>
        <span class="bg-indigo-100 text-indigo-700 px-3 py-1 rounded-full text-sm font-medium">
          #<%= tag %>
        </span>
      <% }) %>
    </div>

    <!-- Back Button -->
    <div class="flex justify-end">
      <a href="/view-post" class="bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-medium px-4 py-2 rounded-lg transition-colors duration-300">
        ← Back to Articles
      </a>
    </div>
  </main>

</body>
</html>
